У МЕНЯ УЖЕ ЕСТЬ РАБОТАЮЩЕЕ ПРИЛОЖЕНИЕ (Spring Boot + HTML/CSS/Vanilla JS) С ДЕРЕВОМ СЛЕВА И КАРТОЧКОЙ УПРАЖНЕНИЯ СПРАВА.
НУЖНО СДЕЛАТЬ UI/UX УЛУЧШЕНИЯ, НЕ ЛОМАЯ API И ФАЙЛОВУЮ СТРУКТУРУ.

ЗАДАЧИ (ОБЯЗАТЕЛЬНО)

1) МОБИЛЬНАЯ ВЕРСИЯ + СКРЫВАЕМАЯ ЛЕВАЯ ПАНЕЛЬ
- Сделай адаптивную верстку (responsive).
- На ширине <= 900px:
  - левая панель (дерево) должна быть "drawer/side sheet": выезжает/уезжает.
  - сверху в header добавь кнопку (иконка “☰” или “←”) которая:
    - если панель закрыта — открывает дерево,
    - если панель открыта — закрывает дерево.
  - При выборе упражнения на мобильном: автоматически закрывай левую панель, чтобы пользователь видел содержимое справа.
- На десктопе: панель по умолчанию видна, но добавь кнопку "свернуть/развернуть" для удобства.

2) СОХРАНЕНИЕ СОСТОЯНИЯ ДЕРЕВА (РАСКРЫТЫЕ ПАПКИ НЕ ДОЛЖНЫ СХЛОПЫВАТЬСЯ)
Сейчас при выборе упражнения или обновлении справа дерево сбрасывается.
Нужно:
- хранить состояние раскрытия узлов (expanded/collapsed) в JS структуре и/или localStorage.
- ключ состояния = path узла.
- при перерисовке дерева:
  - сохраняй текущие раскрытые узлы
  - после обновления дерева восстанавливай раскрытие тех же папок.
- при клике по папке — переключай только эту папку (не сбрасывай всё дерево).
- при клике по упражнению — НЕ трогай раскрытие других папок.

3) КРАСИВЫЕ ПЛЮС/МИНУС И UI ДЕРЕВА
- Вместо текстовых "+/-" сделай аккуратные иконки (можно unicode: "▸" закрыто и "▾" открыто, или SVG).
- Покажи визуально:
  - папка (иконка 📁/🗂️)
  - упражнение (иконка 🥋/📄)
- Подсветка выбранного упражнения (active item).
- Плавная анимация раскрытия (CSS transition, без тяжёлых библиотек).

4) ПРАВАЯ ЧАСТЬ КАК “ПРОВОДНИК”: BREADCRUMBS + СПИСОК ВНУТРИ ПАПКИ
Нужно, чтобы клик по папке/подпапке слева показывал справа “содержимое папки”, как в проводнике:
- сверху справа показывай breadcrumbs:
  KungFu / Basics / ...
  Каждый сегмент кликабельный (возвращает на этот раздел).
- если справа открыт РАЗДЕЛ:
  - показывай список подпапок (разделов)
  - и список упражнений внутри него
  - элементы кликабельны: подпапка открывает раздел, упражнение открывает карточку упражнения.
- если справа открыто УПРАЖНЕНИЕ:
  - показывай как сейчас карточку упражнения.

Для этого:
- расширь фронт: добавь режим правой панели "VIEW_SECTION" и "VIEW_EXERCISE".
- Используй тот же /api/tree, но сделай удобный поиск узла по path в JS.
- НЕ МЕНЯЙ backend API, если это возможно. Если совсем необходимо — можно добавить 1 endpoint:
  GET /api/section?path=... -> {subsections:[...], exercises:[...]}
  Но предпочтительно делать из /api/tree.

5) ПРЕДПРОСМОТР ФАЙЛОВ В СПИСКЕ (THUMBNAILS)
В блоке файлов упражнения:
- Для видео: показывай маленький inline preview:
  - либо мини-плеер <video controls muted preload="metadata" style="max-width: ...">
  - либо превью-плитку (постер) + кнопка открыть.
- Для изображений: показывай thumbnail (<img> с ограничением размеров).
- Для остальных файлов: показывай иконку по типу (pdf, txt, zip).
- При клике “Открыть”:
  - видео — открывать в модальном окне (modal) с большим <video controls> или просто под списком крупно
  - изображения — модал с увеличением
  - прочее — link download/open

Важно: видео большие, поэтому:
- Не пытайся грузить всё заранее.
- Используй existing stream URL.
- Для thumbnail/video preview используй preload="metadata" и muted.

6) ТЁМНАЯ ТЕМА + НОРМАЛЬНЫЕ ЦВЕТА ПОЛЕЙ ВВОДА
Сейчас на мобиле в dark mode может быть “белое по белому”.
Сделай правильную поддержку:
- Используй CSS variables и prefers-color-scheme.
- Определи цвета фона/текста/бордеров/кнопок/inputs/textarea.
- Обязательно: textarea/input имеют контрастный текст и фон в обеих темах.
- Добавь легкий toggle темы (опционально), но как минимум auto по prefers-color-scheme.
- Не используй тяжелые фреймворки. Только CSS.

ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ
- Сохрани существующую структуру проекта и API.
- Правки только во фронтенде (HTML/CSS/JS) и минимально в backend если действительно нужно.
- Сделай код читаемым:
  - отдельные функции: renderTree(), restoreTreeState(), saveTreeState(), renderRightPanelSection(), renderRightPanelExercise()
  - хранение UI state в одном объекте appState: { selectedPath, rightMode, expandedPaths:Set, sidebarOpen, currentSectionPath }
- Состояние expandedPaths сохраняй в localStorage, ключ например "kf_tree_expanded_v1".
- Выбранный путь тоже можно сохранять (опционально).

ПРОВЕРКА/ТЕСТЫ
- Добавь простые frontend self-checks (без тест-фреймворков):
  - при загрузке: если localStorage содержит expandedPaths — восстанови и проверь что элементы раскрылись.
  - логируй в консоль изменения state.
- Backend тесты не трогай, если не меняешь backend.

РЕЗУЛЬТАТ
- После правок приложение должно:
  1) выглядеть нормально на телефоне
  2) левую панель можно скрывать/показывать
  3) состояние дерева не сбрасывается при кликах
  4) справа есть breadcrumbs и режим “папка как проводник”
  5) предпросмотр медиа виден
  6) в dark mode текст в полях ввода читаем

Сделай изменения и обнови README.md коротким разделом "UI/UX (mobile + theme)".
